import 'package:flutter/material.dart';

class SellBooksScreen extends StatelessWidget {
  const SellBooksScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Sell Books'),
      ),
      body: const Center(
        child: Text('Sell Books Screen - Under Construction', style: TextStyle(fontSize: 24)),
      ),
    );
  }
}
