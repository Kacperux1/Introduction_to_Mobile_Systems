import 'package:flutter/material.dart';

class ReviewBooksScreen extends StatelessWidget {
  const ReviewBooksScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Review Books'),
      ),
      body: const Center(
        child: Text('Review Books Screen - Under Construction', style: TextStyle(fontSize: 24)),
      ),
    );
  }
}
