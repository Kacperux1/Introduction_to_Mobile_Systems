import 'package:flutter/material.dart';

class BuyBooksScreen extends StatelessWidget {
  const BuyBooksScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Buy Books'),
      ),
      body: const Center(
        child: Text('Buy Books Screen - Under construction', style: TextStyle(fontSize: 24)),
      ),
    );
  }
}
